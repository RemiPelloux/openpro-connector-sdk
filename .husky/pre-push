#!/usr/bin/env sh
# ------------------------------------------------------------------
# pre-push — openprosdk
#
# Runs TypeScript type-check before pushing.
# ------------------------------------------------------------------
ROOT="$(git rev-parse --show-toplevel)"
cd "$ROOT" || exit 1

pnpm exec tsc --noEmit || exit 1
